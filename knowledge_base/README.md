# База знаний для задания 2

Предметная область: вселенная «Властелина колец».

Что сделано:
1. Подготовлены 32 исходных документа в `rawSources/` (один файл на сущность).
2. Составлен словарь подмены терминов в `terms_map.json` (`оригинал -> вымышленное`).
3. Выполнена сборка через `scripts/build_knowledge_base.py`.
4. Финальные документы записаны в `knowledge_base/`.
5. Отчет валидации сохранен в `knowledge_base/build_report.json`.

Результат валидации:
- документов обработано: 32;
- документов с остатками терминов из словаря: 0.

Как пересобрать:
```bash
python scripts/seed_raw_sources_ru.py
python scripts/build_knowledge_base.py
```
